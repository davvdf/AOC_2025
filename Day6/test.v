//for each column in input file
//operation is on the bottom row, do operation on every entry in the column
//accumulate everything
//declare amount of rows as a constant: for our case it's 4, can parallelize this too
//very parallelizable!
//input stream
//instantiate DSP for FPGA
